import styled from "styled-components";

const RegisterContainer = styled.body`
  background-color: #163c9b;
`;

const Divider = styled.hr`
  border: 0.4px solid #ffffff;
  opacity: 0.2;
`;

export { RegisterContainer, Divider };
