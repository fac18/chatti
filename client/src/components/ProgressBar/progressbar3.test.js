import React from "react";
import ReactDOM from "react-dom";
import ProgressBar3 from "./ProgressBar3";


it("ProgressBar3 renders without problems", () => {
  const div = document.createElement("div");
  ReactDOM.render(<ProgressBar3 />, div);
});

